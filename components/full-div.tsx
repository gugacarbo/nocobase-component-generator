function FullDiv({ children }: { children: React.ReactNode }) {
    //bundle-only: const element = ctx?.element.parentElement.getElementsByTagName("span")?.[0]
    //bundle-only: if (element) {
    //bundle-only: element.style.width = "100%";
    //bundle-only: }
    return (children);
}

export default FullDiv